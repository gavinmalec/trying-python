# python program to calculate gross pay

hours=input('hours done:')
pay=input('pay per hour:')
grosspay=float(hours)*float(pay)
print('Grosspay:',grosspay)


type()-shows variable type
convert float to int: r=3.5,int(r)
convert str to int: t='1234',int(t),float(t) 

